export type FormDatatype = {
  name: string,
  email: string,
  password: string,
  confirmPassword: string
};

export type Errors = {
  name ?: string,
  email ?: string,
  password ?: string,
  confirmPassword ?: string
};
